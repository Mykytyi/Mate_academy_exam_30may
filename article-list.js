'use strict';

class ArticleList {
  constructor(container) {
    this.HtmlElem = document.createElement('container');
  }
  addArticle(article) {
    let one = document.createElement('article');
  }
  removeArticle(article) {
    one.remove();
  }
  render() {

  }
}
